# API

`GET /api/memberships` passa a incluir `usage`, com `membership_id`, `cycle_start`, `cycle_end`, `attended_classes`, `classes_per_month` e `remaining_classes`. Nenhuma mutação adicional é necessária; o cálculo reflete a presença já registrada.
